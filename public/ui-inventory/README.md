# UI Inventory

**Latest developer-facing inventory.** Use this to verify component names, states, radii,
icons, and behavior before adding a new page-level pattern.

Open `ui-components.html`. Keep the CSS, JavaScript, `assets/`, and `i18n/` folders together.

The product links at the top open the current B2C and CMS files.
